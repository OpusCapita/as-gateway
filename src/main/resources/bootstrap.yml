spring:
    application:
        name: as-gateway
    cloud:
        consul:
            host: consul
            port: 8500

---
spring:
    profiles: local
    cloud:
        consul:
            enabled: false

---
spring:
    profiles: test
    cloud:
        discovery:
            enabled: false
        consul:
            enabled: false
            config:
                enabled: false
